import { Component } from '@angular/core';

/** Static system health strip shown at the bottom of module pages. */
@Component({
  selector: 'app-system-status-bar',
  templateUrl: './system-status-bar.html',
})
export class SystemStatusBar {}
